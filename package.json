{
  "name": "codex-plugins-market-audit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reproducible audit and portable dashboard for Codex and ChatGPT plugin markets.",
  "scripts": {
    "collect": "node scripts/collect.mjs",
    "artifact": "node scripts/build-artifact.mjs",
    "validate": "node scripts/validate-snapshot.mjs && node --test tests/*.test.mjs",
    "report:deliver": "bash scripts/deliver-report.sh",
    "report:verify-enhancements": "node scripts/verify-report-enhancements.mjs",
    "build": "npm run artifact && npm run validate && npm run report:deliver && npm run report:verify-enhancements",
    "refresh": "npm run collect && npm run build"
  }
}
